{
  "control_id": "workflow_queue_length",
  "version": "1.0.0",
  "title": "Workflow Queue Status / İş Akışı Sırası Durumu",
  "description": "Shows workflow queue depths and pending item ages",
  "intent": "workflow",
  "keywords": {
    "en": [
      "queue",
      "workflow",
      "notification",
      "bildirim"
    ],
    "tr": [
      "sıra",
      "iş akışı"
    ]
  },
  "queries": [
    {
      "query_id": "wf_queue_status",
      "sql": "SELECT COUNT(*) queue_depth, 'TOTAL' queue_type, MIN(creation_date) oldest_date FROM wf_item_activity_statuses WHERE queue_type IN ('WAITING', 'NOTIFIED')",
      "binds": [],
      "row_limit": 50,
      "timeout_seconds": 30,
      "result_schema": [
        {
          "name": "QUEUE_DEPTH",
          "type": "VARCHAR2",
          "sensitive": false
        },
        {
          "name": "QUEUE_TYPE",
          "type": "VARCHAR2",
          "sensitive": false
        },
        {
          "name": "OLDEST_DATE",
          "type": "DATE",
          "sensitive": false
        }
      ]
    }
  ],
  "doc_hint": "Shows workflow queue depths. High WAITING/NOTIFIED queues or old deferred items indicate workflow processing delays.",
  "analysis_prompt": "Check workflow queues. Report: (1) queue depths by type, (2) age of oldest pending items, (3) if deferred queue is backed up, (4) recommend investigating workflow processes.",
  "safety_classification": "SAFE_READONLY"
}